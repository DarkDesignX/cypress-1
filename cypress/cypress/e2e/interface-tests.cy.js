describe ("if not signed in", () => {
    it("returns only ACTIVE products on the home page", () => {
		
	})
})

describe ("if signed in", () => {
	it("returns only ACTIVE products on the home page", () => {
		
	})

	it('it return ALL products on the products page', () => {

	})
})